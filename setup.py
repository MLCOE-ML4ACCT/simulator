from setuptools import find_packages, setup

setup(
    name="simulator",
    version="0.1.0",
    packages=find_packages(),
    description="A dynamic microeconometric simulation model for incorporated businesses.",
)
